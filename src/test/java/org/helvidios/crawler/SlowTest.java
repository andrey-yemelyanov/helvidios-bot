package org.helvidios.crawler;

/**
 * Slow tests usually run against real dependencies that involve network or other IO communication with external resources.
 * These tests are excluded from the build.
 */
public interface SlowTest {
    
}
